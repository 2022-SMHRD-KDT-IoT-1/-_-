<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Model.LoactionDAO"> <!-- mapper를 누가사용할것인ㄴ지 정해줌 -->

	 <select id = "ViewLocation"  resultType = "Model.LocationVO">
		select * from jp_sos
		where sos_seq = #{sos_seq}
	 </select>

</mapper>
