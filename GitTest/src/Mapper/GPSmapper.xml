<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Model.MemberDAO"> <!-- mapper를 누가사용할것인ㄴ지 정해줌 -->


	<insert id="GPSload" parameterType="Model.GpsVO">
		insert into jp_location values(jp_location_seq.nextval, #{mem_id}, #{latitude},#{longitude},#{loc_memo},sysdate)
	</insert>

	<select id ="GPSdetect"	resultType = "Model.CommunityVO">
		select * from jp_community
		where mem_id = #{mem_id}
	</select>
	
	
	
</mapper>