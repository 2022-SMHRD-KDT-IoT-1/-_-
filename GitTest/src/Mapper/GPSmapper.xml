<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Model.MemberDAO"> <!-- mapper를 누가사용할것인ㄴ지 정해줌 -->


	<insert id="joinService" parameterType="Model.MemberVO">
		insert into jp_location values(#{mem_id},#{mem_pw},#{mem_name},#{mem_gender},#{mem_birthdate},#{mem_tel},#{mem_guardian_tel},#{jp_num},sysdate)
	</insert>

	
	
</mapper>